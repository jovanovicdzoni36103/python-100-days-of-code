"""
TASK 01 - Funkcija sa opcionim pragom                     [Level 3]

Napravi funkciju classify(score, threshold=0.5) koja vraća "positive"
ako je score >= threshold, inače "negative".

Pozovi je jednom bez threshold (koristi podrazumevanu vrednost) i
jednom sa threshold=0.8, za isti score=0.6.

AI/ML: Prag za klasifikaciju se često menja u zavisnosti od projekta (koliko strog želiš da bude model). Default vrednost + mogućnost da je promeniš je standardan obrazac.
"""

# Rešenje:
