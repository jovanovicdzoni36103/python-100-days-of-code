# day 008 - caesar cipher

honestly today went way smoother than that hangman nightmare. functions finally make sense and building the cipher was actually fun. modulo saved my life for wrapping the index when 'z' goes out of bounds, and instead of writing two separate functions i just flipped the shift to negative for decoding so one loop handles everything. also made sure spaces and numbers don't crash the code and wrapped the whole thing in a while loop so i don't gotta keep restarting the script manually. 