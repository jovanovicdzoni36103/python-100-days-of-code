# Day 3: Treasure Island

| | |
|---|---|
| Learning Day | 4 |
| Course Day | Angela Yu Day 3 |
| Topic | Control flow, logical operators, nested conditionals |
| Status | Completed |

Today was mostly if, elif and else. It was not too hard, I have done this kind of thing in other languages already.

BMI passed on the first try. The pizza order program was harder. I knew right away that I needed `+=`, that `bill` starts at zero and that everything gets added onto it. The logic was fine, the syntax was where I lost time. I wrote a statement right after the colon and then tried to indent more code under it, and Python threw `IndentationError: unexpected indent`. The block was already closed by then.

The hardest one was Treasure Island. The good thing was that the course gave me a flowchart, so I knew exactly what to do and which logic to follow, and I could just write the code. I got a little better at working out how the project should go before I start writing it, so I can see the whole picture first.

I still write `else if` out of JavaScript habit instead of `elif`. That will wear off.

Today took a bit more time than I expected, but most of it went well.

Tomorrow: Randomisation and Python Lists
